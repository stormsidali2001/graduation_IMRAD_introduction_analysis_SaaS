{
  "name": "imrad-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev:web": "turbo run dev --filter=web",
    "dev:user-data": "turbo run dev --filter=user-data",
    "dev:js": "turbo run dev --filter=web --filter=user-data"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.0.0"
}
